<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FormulaOne.SplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">#050508</item>

        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
             animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_monochrome</item>
        <!-- Required for animated icons -->
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/Theme.Material.DayNight.NoActionBar</item>
    </style>

    <style name="Theme.Material.DayNight.NoActionBar" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <style name="Theme.GameWish" parent="@style/Theme.Material.DayNight.NoActionBar">
        <item name="android:colorPrimary">#B82E2E</item>
        <item name="android:colorAccent">#B82E2E</item>
    </style>
</resources>
